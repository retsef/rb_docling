# Dipendenze per la conversione PyTorch → ONNX dei modelli Docling.
# Solo per uso CONVERSIONE (one-time, lato sviluppatore).
# Gli utenti di rb_docling NON devono installare nulla di tutto questo.
#
# Uso:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r tools/requirements.txt
#   python tools/export_layout.py
#   python tools/export_tableformer.py

# Core ML
torch>=2.2,<2.6
onnx>=1.16
onnxruntime>=1.18

# Pesi originali Docling
huggingface_hub>=0.23
safetensors>=0.4

# Architetture custom (TableFormer, Heron layout)
# Pin di sicurezza: rilasci ds4sd recenti possono introdurre breaking change
# nel naming dei moduli (vedi export_*.py per i punti TODO da rivisitare).
docling-ibm-models>=2.0

# Utility per il diagnostic dump dei grafi ONNX
onnx-simplifier>=0.4
